<Window x:Class="ServicoInteligenteGeografico.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServicoInteligenteGeografico"
        mc:Ignorable="d"
        Title="Serviço inteligente Greográfico" Height="566" Width="800">

    <!-- Estilos e recursos da janela -->
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" >
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Efeito Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Layout principal dividido em duas colunas -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition Width="151*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            
            <TextBlock Text="Buscar Localização:" FontWeight="Bold" FontSize="14" Margin="6" ></TextBlock>
            <TextBox Margin="6" Padding="6" Text="{Binding Localizacao, UpdateSourceTrigger=PropertyChanged}" />

            <Button Style="{StaticResource ButtonStyle}" Content="Buscar" FontWeight="Bold" Margin="6" Padding="6" Command="{Binding BuscarCommand}" Height="38" />

            <ListBox Margin="6" Height="213" ItemsSource="{Binding Temperaturas}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat={}{0}°C}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="Distância entre pontos:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Longitude:" Margin="6"></TextBlock>
                    <TextBox Margin="6" Padding="6" Text="{Binding Longitude, UpdateSourceTrigger=PropertyChanged}" Width="168" />
                </StackPanel>

                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Latitude:" Margin="6"></TextBlock>
                    <TextBox Margin="6" Padding="6" Text="{Binding Latitude, UpdateSourceTrigger=PropertyChanged}" Width="177" />
                </StackPanel>
            </StackPanel>
            
            <Button Style="{StaticResource ButtonStyle}" Content="Calcular" FontWeight="Bold" Margin="6" Padding="6" Command="{Binding CalcularCommand}" Height="38" />
            <TextBlock Text="{Binding ResultadoFormatado}" FontSize="16" Foreground="Green" Margin="6" />

        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Filtro por Região:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <ComboBox Margin="6" Padding="6" SelectedIndex="0">
                <ComboBoxItem Content="Cidade" />
                <ComboBoxItem Content="Bairro" />
                <ComboBoxItem Content="Rua" />
                <ComboBoxItem Content="País" />
                <ComboBoxItem Content="Estado" />
            </ComboBox>

            <TextBlock Text="Geração de Relatórios:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ButtonStyle}" Content="Gerar PDF" Margin="6" Padding="6" Width="190" Height="36" FontWeight="Bold" Command="{Binding GerarPdfCommand}" />
                <Button Style="{StaticResource ButtonStyle}" Content="Histórico" Margin="6" Padding="6" Width="183" Height="36" FontWeight="Bold" Command="{Binding HistoricoCommand}" />
            </StackPanel>
            
            <TextBlock Text="Ranking:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <ScrollViewer Margin="6">
                <DataGrid Margin="6" Height="150" 
                          ItemsSource="{Binding Resultados}" 
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single">   
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                        <DataGridTextColumn Header="Logradouro" Binding="{Binding Logradouro}" Width="1*"/>
                        <DataGridTextColumn Header="Nº" Binding="{Binding Numero}" Width="Auto"/>
                        <DataGridTextColumn Header="Bairro" Binding="{Binding Bairro}" Width="*"/>
                        <DataGridTextColumn Header="CEP" Binding="{Binding Cep}" Width="Auto"/>
                        <DataGridTextColumn Header="Lat" Binding="{Binding Latitude, StringFormat={}{0:F4}}" Width="Auto"/>
                        <DataGridTextColumn Header="Long" Binding="{Binding Longitude, StringFormat={}{0:F4}}" Width="Auto"/>
                        <DataGridTextColumn Header="Data/Hora" Binding="{Binding Timestamp}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </StackPanel>

    </Grid>
</Window>
