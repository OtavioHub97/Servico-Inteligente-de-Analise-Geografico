<Window x:Class="ServicoInteligenteGeografico.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServicoInteligenteGeografico"
        mc:Ignorable="d"
        Title="Serviço inteligente Greográfico" Height="450" Width="800">

    <!-- Estilos e recursos da janela -->
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" >
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Efeito Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Layout principal dividido em duas colunas -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition Width="151*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            
            <TextBlock Text="Buscar Localização:" FontWeight="Bold" FontSize="14" Margin="6" ></TextBlock>
            <TextBox Margin="6" Padding="6" Text="{Binding Localizacao, UpdateSourceTrigger=PropertyChanged}" />

            <Button Style="{StaticResource ButtonStyle}" Content="Buscar" FontWeight="Bold" Margin="6" Padding="6" Command="{Binding BuscarCommand}" Height="38" />

            <ListBox Margin="6" Height="213" ItemsSource="{Binding Temperaturas}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat={}{0}°C}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Filtro por Região:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <ComboBox Margin="6" Padding="6" SelectedIndex="0">
                <ComboBoxItem Content="Cidade" />
                <ComboBoxItem Content="Bairro" />
                <ComboBoxItem Content="Rua" />
                <ComboBoxItem Content="País" />
                <ComboBoxItem Content="Estado" />
            </ComboBox>

            <TextBlock Text="Geração de Relatórios:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ButtonStyle}" Content="Gerar PDF" Margin="6" Padding="6" Width="190" Height="36" FontWeight="Bold" Command="{Binding GerarPdfCommand}" />
                <Button Style="{StaticResource ButtonStyle}" Content="Histórico" Margin="6" Padding="6" Width="183" Height="36" FontWeight="Bold" Command="{Binding HistoricoCommand}" />
            </StackPanel>
            
            <TextBlock Text="Ranking:" FontWeight="Bold" FontSize="14" Margin="6"></TextBlock>
            <ScrollViewer Margin="6">
                <DataGrid Margin="6" Height="150" ItemsSource="{Binding Resultados}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="Logradouro" Binding="{Binding Logradouro}" />
                        <DataGridTextColumn Header="Número" Binding="{Binding Numero}" />
                        <DataGridTextColumn Header="Bairro" Binding="{Binding Bairro}" />
                        <DataGridTextColumn Header="Cep" Binding="{Binding Cep}" />
                        <DataGridTextColumn Header="Latitude" Binding="{Binding Latitude}" />
                        <DataGridTextColumn Header="Longitude" Binding="{Binding Longitude}" />
                        <DataGridTextColumn Header="Tempo" Binding="{Binding Tempo}" />
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </StackPanel>

    </Grid>
</Window>
